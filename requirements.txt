# COGNIFY — shared Python runtime (all services: core-backend, ai-service, execution-service)
# Scaffold only, no business logic yet.
# Install:  python -m pip install -r requirements.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1
sqlalchemy==2.0.54
