# execution-service local env (copy to .env — not used yet, scaffold only)
ENV=development
LOG_LEVEL=info
PORT=8002
CORE_BACKEND_URL=http://localhost:8000
EXECUTION_TIMEOUT_SECONDS=5
EXECUTION_MAX_OUTPUT_BYTES=102400
